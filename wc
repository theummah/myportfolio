I tried to create a GitHub page for my 'myportfolio' repo using settings. Then manually on the command line. It never works, I get a non-fast-forward error when attempting to do it manually. I've tried googling it as well as searching stack overflow. Nothing works. -m
